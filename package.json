{
  "name": "echo360-speed-control",
  "version": "1.3.2",
  "description": "Override Echo360 playback speed restrictions",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "cp -r assets dist/ && cp manifest.json dist/ && cp src/popup/popup.html dist/popup/ && cp src/popup/popup.css dist/popup/",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5"
  }
}
